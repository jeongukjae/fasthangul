#include "fasthangul/normalizer.hh"
#include <algorithm>

// std::wstring normalizerWhitespace(std::wstring_view text) {
//   std::wstring cleanText;
//   std::copy_if(text.begin(), text.end(), cleanText.begin(), [](const wchar_t &character) {});
// }
